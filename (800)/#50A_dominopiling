#include<iostream>

using namespace std;

int main()
{
    int m, n;
    cin >> m >> n;

    int p = m * n;
    int ans;

    if (p % 2 == 0)
        ans = p / 2;
    else
        ans = (p - 1) / 2;

    cout << ans;

    return 0;
}
